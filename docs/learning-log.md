# 制作・学習記録

## 2026年7月13日

### 今日の作業

- スマホで発生していた横スクロールを修正した
- ページ装飾画像が非表示になる原因を確認した
- ReserveページにFAQを追加した
- Reserveページを複数の`section`に整理した
- FAQにCSSを適用した
- セクション間の余白を調整した
- PCとAndroidのキャッシュ対策を確認した
- GitHub Pagesの公開版を確認したv

### 1. 横スクロールと`overflow`

装飾画像を画面の左右から少しはみ出させると、ページ全体の横幅が広がり、横スクロールが発生することがある。

横方向だけを切り、縦方向の装飾画像は表示したい場合は、次のように指定する。

```css
.page-illusts {
  overflow-x: clip;
  overflow-y: visible;
}
```

`overflow: hidden`は横方向だけでなく、縦方向にはみ出した画像も非表示にする。

### 2. `section`の使い方

`section`の中に`section`を入れること自体は、HTMLのルール上は禁止されていない。

ただし、予約・FAQ・来店前のお願いがそれぞれ独立した内容なら、兄弟関係にした方が構造を理解しやすい。

```html
<main>
  <section class="section reserve-section">
    <!-- 予約について -->
  </section>

  <section class="section faq-section">
    <!-- FAQ -->
  </section>

  <section class="section visit-notice-section">
    <!-- ご来店前のお願い -->
  </section>
</main>
```

### 3. FAQに適したHTMLタグ

質問と回答の組み合わせには、説明リストを表すタグが適している。

- `dl`：質問と回答の一覧全体
- `dt`：質問
- `dd`：回答

```html
<dl class="faq-list">
  <div class="faq-item">
    <dt>予約は必要ですか？</dt>
    <dd>事前予約制としています。</dd>
  </div>
</dl>
```

「Q.」「A.」はHTMLへ直接書かず、CSSの疑似要素で表示できる。

```css
.faq-item dt::before {
  content: "Q.";
}

.faq-item dd::before {
  content: "A.";
}
```

デザイン用の文字をCSSで付けることで、HTML本文を読みやすく保てる。

### 4. CSSを書く位置

FAQのCSSは、関連するReserveのCSSの後、FooterのCSSの前へまとめると探しやすい。

```text
Reserve関連
↓
FAQ関連
↓
Footer関連
↓
スマホ用の@media
```

CSSは別の位置でも動作するが、HTMLの表示順に近い順番で整理すると保守しやすい。

### 5. セクションの余白

共通の上下余白は、`.section`の`padding`で指定している。

```css
.section {
  padding: 90px 28px;
}
```

この場合、最初の数値が上下、2番目の数値が左右を表す。

```css
padding: 上下 左右;
```

複数の`section`を連続させると、前の`section`の下余白と、次の`section`の上余白が続くため、間隔が広く見える。

特定のページだけ狭くする場合は、個別に上書きする。

```css
.reserve-section,
.faq-section,
.visit-notice-section {
  padding-top: 60px;
  padding-bottom: 60px;
}
```

### 6. キャッシュについて

ローカルでは直っていても、公開サイトではブラウザに古いCSSが残ることがある。

PC版Chromeでは、最初に次の強制再読み込みを試す。

```text
Ctrl + F5
```

開発中は、Chromeの開発者ツールを使う方法もある。

1. `F12`を押す
2. `Network`を開く
3. `Disable cache`にチェックする
4. 開発者ツールを開いたまま再読み込みする

CSSのURLにバージョンを付ける方法もある。

```html
<link rel="stylesheet" href="css/style.css?v=20260713">
```

この方法は一般的だが、複数のHTMLで番号が違わないように注意する。制作中は強制再読み込みを使い、最終公開時や大きな更新時だけバージョンを変更すると安全である。

### 7. 架空サイトとしての見せ方

FAQ本文では、実在する店舗の設定として自然に言い切る。

```text
事前予約制としています。
```

「想定しています」を繰り返すと、未完成の文章に見えやすい。

代わりに、フッターなどへ次の注記を掲載する。

```text
このWebサイトは、学習・ポートフォリオ制作を目的とした架空の店舗サイトです。
```

### 今日の重要ポイント

- 表示が崩れたときは、HTML構造、CSS、キャッシュの順に確認する
- 装飾画像のはみ出しには`overflow`が関係する
- 内容が独立している場合は`section`を分ける
- 質問と回答には`dl`、`dt`、`dd`を使える
- 共通CSSと個別CSSを使い分ける
- 公開版だけ古い場合は、最初に`Ctrl + F5`を試す

### 次回の作業

- FigmaでFAQの開閉状態を設計する
- 常時表示とアコーディオン形式を比較する
- 開閉ボタンのデザインを作る
- スマホでの押しやすさを確認する
- 実装前に通常時・開いた状態・閉じた状態を設計する

## 2026年7月14日

### 今日の作業

- ReserveページにFAQアコーディオンを実装した
- `details`と`summary`の役割を確認した
- Reserveページの文章量と上下余白を見直した
- Googleマップの埋め込みコードを確認した
- `map-placeholder`と`map-embed`の違いを確認した
- Piggiesページの絵文字を見直した
- Homeの装飾イラストを名前の横へ配置する方法を検討した

### 1. FAQアコーディオン

クリックすると質問の回答が開閉する仕組みを「FAQアコーディオン」と呼ぶ。

JavaScriptを使用しなくても、HTMLの`details`と`summary`で開閉機能を実装できる。

```html
<details class="faq-item">
  <summary class="faq-question">
    予約は必要ですか？
  </summary>

  <div class="faq-answer">
    <p>事前予約制としています。</p>
  </div>
</details>
```

- `details`：質問と回答をまとめる
- `summary`：クリックする質問部分
- `details`1個につき、質問と回答を1組入れる
- `summary`は`details`の最初に置く

`summary`を複数まとめて1個の`details`へ入れないように注意する。

### 2. FAQのHTML変更とCSS

FAQを`dt`・`dd`から`details`・`summary`へ変更した場合、以前のCSSは適用されなくなる。

```css
.faq-item dt {
  /* details形式では適用されない */
}
```

新しいクラスに合わせて指定する。

```css
.faq-question {
  /* 質問部分 */
}

.faq-answer {
  /* 回答部分 */
}
```

### 3. セクションの余白

スマホでは`.section`に上下`70px`の余白が設定されていた。

複数のセクションが続くと余白が広く見えるため、Reserveページだけ個別に調整すると安全である。

```css
@media (max-width: 768px) {
  .reserve-section,
  .faq-section,
  .visit-notice-section {
    padding-top: 48px;
    padding-bottom: 48px;
  }
}
```

共通の`.section`を変更すると、ほかのページにも影響するので注意する。

### 4. Googleマップの埋め込み

`src`はタグではなく、`iframe`の属性として開始タグの中へ記述する。

```html
<div class="map-embed">
  <iframe
    src="GoogleマップのURL"
    width="600"
    height="450"
    loading="lazy">
  </iframe>
</div>
```

次のような`src`タグは存在しない。

```html
<iframe>
  <src="GoogleマップのURL">
</iframe>
```

HTMLを修正するときは、余分な`</div>`が増えていないかも確認する。

### 5. `map-placeholder`と`map-embed`

- `map-placeholder`：地図を入れる前の仮表示
- `map-embed`：実際の地図を埋め込むための囲い

これらはHTML標準の名前ではなく、制作者が目的に合わせて付けたクラス名である。

実際の地図へ交換した後は、基本的に`map-embed`だけを使用する。

### 6. 絵文字とイラストの見直し

Piggiesページでは、ウリ坊とぶちを見分けるために絵文字を使用していた。

```html
<div class="piggy-icon">🐗</div>
<div class="piggy-icon">🐷</div>
```

現在は写真と名前があるため、絵文字の役割が重複している。また、絵文字は端末によって見た目が変わる。

サイトの統一感を出すため、Homeで使用しているイラストへ置き換える方針とした。

名前とイラストを横並びにする場合は、同じ要素で囲む。

```html
<div class="piggy-name">
  <h2>ウリ坊</h2>
  <img
    class="piggy-name-illust"
    src="img/ChatGPT_bg_12.png"
    alt="">
</div>
```

```css
.piggy-name {
  display: flex;
  align-items: center;
  gap: 12px;
}
```

Homeの自由配置に使用している`.float-illust`は、プロフィール内では使わない。自由配置用のCSSによって、イラストがカードの外へ移動する可能性がある。

### 今日の重要ポイント

- 開閉式のFAQは「FAQアコーディオン」と呼ぶ
- `details`1個につき質問と回答を1組入れる
- HTMLを変更したらCSSのセレクターも確認する
- 特定ページの余白は個別クラスで調整する
- `iframe`の`src`は開始タグの属性として書く
- 絵文字より既存イラストを使うと統一感が出る

### 次回の作業

- ウリ坊とぶちの名前を縦書きにする
- 名前の横へ既存イラストを配置する
- PCとスマホでプロフィールのバランスを確認する
- GitHub Pagesへ反映し、公開版を確認する

## 2026年7月15日

### 今日の作業

- TOPページのPiggiesプロフィールにスクロールアニメーションを追加した
- ウリ坊を左、ぶちを右から表示する構成にした
- 写真、名前、プロフィール文章の順に表示されるよう遅延を設定した
- プロフィール文章へ、一行ごとのマーカー風背景を付けた
- 横移動を大きくし、写真へわずかな傾きを付けてメリハリを強めた
- 上スクロール時にも、遅延付きでスライドアウトする動きを設定した
- About、Movie、Gallery、Access、Reserveにも同じトーンの動きを展開した
- 公開サイトをPC・スマホ表示で確認し、納品までの進捗を整理した

### 1. スクロールアニメーション

今回実装した動きは、要素が画面内に入ったことを検知して表示する「スクロールアニメーション」である。

JavaScriptで表示範囲への出入りを検知し、CSSのクラスを切り替える。

```text
画面外：通常のクラスだけが付いている
画面内：is-visibleまたはis-revealedが追加される
```

CSSでは、画面外の初期状態と画面内の表示状態を分けて設定する。

```css
.piggy-card {
  opacity: 0;
  transform: translateX(-100vw) rotate(-8deg);
  transition: opacity 2s ease, transform 2s ease;
}

.piggy-card.is-visible {
  opacity: 1;
  transform: translateX(0) rotate(-1.5deg);
}
```

### 2. フェードとスライドの違い

- フェードイン：透明な状態から徐々に見える動き
- フェードアウト：表示状態から徐々に透明になる動き
- スライドイン：画面外や離れた位置から入ってくる動き
- スライドアウト：元の位置から画面外へ移動する動き

今回、速く感じていたのは透明度の変化よりも、横方向の移動だった。動きを調整するときは、`opacity`と`transform`を分けて考える。

```css
transition:
  opacity 2s ease,
  transform 2.4s cubic-bezier(0.22, 1, 0.36, 1);
```

### 3. 表示順を作る`transition-delay`

写真、名前、プロフィール文章を同時に表示せず、少しずつ遅らせることで視線の流れを作った。

```text
写真
↓
名前
↓
プロフィール文章
```

```css
.piggy-card.is-visible .piggy-name {
  transition-delay: 0.65s;
}

.piggy-card.is-visible .piggy-profile {
  transition-delay: 1.05s;
}
```

`transition-duration`は動きにかかる時間、`transition-delay`は動き始めるまでの待ち時間である。

### 4. 一行ごとのマーカー背景

文章全体を四角い背景で囲むのではなく、文字のある一行ごとに背景を付けた。

```css
.piggy-marker-text {
  display: inline;
  padding: 0.12em 0.35em;
  background: linear-gradient(
    transparent 10%,
    rgba(255, 248, 232, 0.88) 10%,
    rgba(255, 248, 232, 0.88) 90%,
    transparent 90%
  );
  box-decoration-break: clone;
  -webkit-box-decoration-break: clone;
}
```

`box-decoration-break: clone`を使うと、改行された各行へ`padding`と背景が繰り返し適用される。

### 5. 上スクロール時の動き

下スクロール時はスライドイン、上スクロール時はスライドアウトするようにした。

上スクロール時にも遅延を付けることで、急に消えず、写真・名前・文章がゆったり退出する印象になった。

ただし、すべての要素へ大きな動きを付けると主役が分かりにくくなる。Piggiesを最も大きく動かし、ほかのセクションは移動距離や傾きを控えめにすると、サイト全体のメリハリを保ちやすい。

### 6. スマホ表示と動きへの配慮

画面外から大きく移動する要素は、一時的にページ幅を広げる可能性がある。横スクロールが発生していないか、実機でも確認する。

動きを減らす設定を使用している人へ配慮する場合は、次の指定を用意する。

```css
@media (prefers-reduced-motion: reduce) {
  * {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
  }
}
```

### 7. 現在の完成度と納品前の確認

公開サイトを確認した結果、Webサイト単体では約82％、Figmaや提出資料まで含めると約72〜75％の進捗と整理した。

現在は大きく作り直す段階ではなく、TOPページの完成度を基準に、ほかのページと細部をそろえる段階である。

納品前に確認する内容は次のとおり。

- フッターへ架空サイトであることを明記する
- Reserveの仮メールアドレスを整理する
- 画像の`alt`を自然な説明へ修正する
- Googleマップの`iframe`へ`title`を付ける
- TOP以外のページをTOPと同じデザイントーンへ整える
- PC、スマホ、タブレットで表示と動きを確認する
- ローディング時間やアニメーションが長すぎないか確認する

### 今日の重要ポイント

- 透明度の変化はフェード、位置の変化はスライドと呼ぶ
- `transition-duration`と`transition-delay`は役割が異なる
- 表示順を付けると、見る人の視線を誘導できる
- 画面外から動かすときは横スクロールに注意する
- 大きな動きは主役へ使い、ほかの要素は控えめにする
- 見た目が完成しても、アクセシビリティや納品表記の確認が必要である

### 次回の作業

- 全ページのフッターへ架空サイトの注記を追加する
- Reserveの予約ボタンとメールアドレスを納品用に整える
- 画像の`alt`とGoogleマップの`title`を確認する
- TOP以外のページの見出し、余白、装飾を統一する
- スマホ、タブレット、PCで最終表示を確認する

## 2026年7月16日

### 今日の作業

- 上スクロール時に情報が消えるスライドアウトを見直した
- 一度表示した情報を残し、画面内でゆっくり漂わせる動きへ変更した
- Piggiesが表示されなくなる原因を確認し、表示判定を修正した
- About、Piggies、Gallery、Access、Reserveの動きのトーンを統一した
- 全ページ共通の`page-motion.js`を作成した
- Reserveページへ`ChatGPT_bg_15`のアイキャッチを追加した
- アイキャッチの背景を透明にし、画像内の透明余白を切り詰めた
- Reserveページの上下余白を調整した
- HTML、CSS、JavaScript、画像、動画を統合した完全版を作成した
- ローカルファイルの参照先とJavaScriptの構文を確認した

### 1. 動きと情報の読みやすさ

上スクロール時に要素を画面外へ戻す動きは、演出としては目立つが、読み返したい情報まで消えてしまう。

今回は次の動きへ変更した。

```text
初めて画面内に入る
↓
スライドインして表示する
↓
表示済みの状態を維持する
↓
画面内にある間だけ、ゆっくり漂わせる
```

見た目だけでなく、利用者が文章を確認しやすいかも考えてアニメーションを決めることが重要である。

### 2. `IntersectionObserver`と画面外の要素

Piggiesは初期状態で、次のように左右の画面外へ移動していた。

```css
.piggy-card:nth-child(1) {
  transform: translateX(-100vw) rotate(-8deg);
}

.piggy-card:nth-child(2) {
  transform: translateX(100vw) rotate(8deg);
}
```

このカード自体を`IntersectionObserver`で監視すると、カードが画面外にあるため「画面内へ入っていない」と判定され、表示用クラスが付かない場合がある。

そこで、移動していない`.piggies-section`を監視し、セクションが画面内へ入ったら2枚のカードへ`is-visible`を付けるようにした。

```javascript
if (target === piggiesSection && entry.isIntersecting) {
  piggyCards.forEach((card) => {
    card.classList.add("is-visible", "is-floating");
  });
}
```

### 3. 表示状態と漂う状態を分ける

表示状態とアニメーション状態を別のクラスで管理した。

- `is-visible`・`is-shown`：表示済みであることを表す
- `is-floating`：現在、画面内で漂っていることを表す

画面外へ出たときは`is-floating`だけを外し、表示用クラスは残す。

```javascript
target.classList.remove("is-floating");
```

これにより、上スクロールしても情報は消えず、画面外では不要なアニメーションを止められる。

### 4. 全ページ共通のJavaScript

各ページへ同じJavaScriptを繰り返し書くのではなく、次の共通ファイルへまとめた。

```text
js/page-motion.js
```

各HTMLでは、`body`の終了タグ直前から読み込む。

```html
<script src="js/page-motion.js"></script>
```

共通ファイルへまとめると、動きの速さや表示判定を一か所で修正できる。ただし、GitHubへアップロードするときは`js`フォルダも忘れないようにする。

### 5. 透明背景画像と透明余白

画像の背景を透明にしても、画像ファイルのキャンバスに透明な上下余白が残っていると、HTML上では大きな空白として扱われる。

今回は次の順番で調整した。

```text
背景を透明にする
↓
透明部分の範囲を確認する
↓
2匹の周囲へ少し安全な余白を残して切り詰める
↓
CSSのmarginとpaddingを調整する
```

表示上の余白が広い場合は、CSSだけでなく画像ファイル内の透明部分も確認する。

### 6. CSSの記述順と余白

スマホ用CSSでは、個別の`.reserve-section`より後ろに共通の`.section`が書かれていると、後ろの指定が優先されて余白が再び広くなる場合がある。

```css
.section {
  padding: 70px 22px;
}

.reserve-section {
  padding: 30px 22px 48px;
}
```

個別調整を確実に反映したい場合は、同じメディアクエリ内で共通指定より後ろに書く。

### 7. 完全版のファイル構成

本日の変更を次の構成へ統合した。

```text
uribou-buchi-complete-20260716
├─ index.html
├─ about.html
├─ piggies.html
├─ gallery.html
├─ access.html
├─ reserve.html
├─ css
│  └─ style.css
├─ js
│  └─ page-motion.js
├─ img
├─ docs
├─ logo.svg
├─ loading.svg
└─ 動画・写真ファイル
```

GitHubへ反映するときは、ZIPファイルではなく、解凍したフォルダの中身をアップロードする。

### 今日の重要ポイント

- アニメーションは情報の読みやすさを妨げないようにする
- 画面外へ移動した要素自身を表示判定に使うと、検知できない場合がある
- 表示状態と、現在動いている状態は別のクラスで管理できる
- 全ページ共通の処理は外部JavaScriptへまとめると修正しやすい
- 透明画像にも、見えない余白が残っている場合がある
- CSSはセレクターだけでなく、記述順も確認する
- 完全版にはHTMLだけでなく、CSS、JavaScript、画像、動画も必要である

### 次回の作業

- これまで作成したフォルダとZIPの役割を整理する
- `uribou-buchi-complete-20260716`を今後の基準ファイルに決める
- 古い試作版と現在使用する完全版を分ける
- GitHubへアップロードするファイルを確認する
- 公開サイトで全6ページの表示とリンクを確認する
- PCとスマホでアニメーションと余白を確認する
- 整理が終わるまでは、新しい装飾を追加しない

## 2026年7月17日

### 今日の作業

- これまでの制作ファイルと公開サイトの役割を整理した
- 全6ページのフッターを共通デザインへ整えた
- フッター上部へカフェ写真の横スライド・コラージュを追加した
- ロゴ、サイト名、コピーライト、ページリンク、架空サイトの注記を整理した
- Reserveへの導線を、右端から少しはみ出す流体形の固定ボタンとして調整した
- Reserveページの予約ボタンを復活させ、予約・お問い合わせフォーム周辺を整理した
- Reserveページのアイキャッチ下にあった大きな空白を調整した
- Galleryの仮文字カードとTOPページのGalleryをカフェ写真へ更新した
- Accessページへ、右上から左下へ現れる足跡の演出を追加した
- TOPページのヒーロー動画直下へ、曜日ごとの営業案内を追加した
- 営業日、定休日、営業時間、Instagramへの案内をサイトの色に合わせた
- 文字の太さを比較し、全体を太くする案は使用せず元の軽さを残した
- 営業案内の背景、形、線、丸の見せ方を複数比較した
- GitHubへ手動アップロードする内容と変更コメントを確認した

### 1. 共通フッターの役割

フッターは、すべてのページの最後に表示される共通部分である。今回は情報を次の3つへ分けた。

```text
左：ロゴ、サイト名、コピーライト
中央：ページリンク、架空サイトであることの注記
右：Reserveボタン
```

情報の役割ごとにまとまりを作ると、項目数が多くても読みやすくなる。ボタンと文字が重ならないよう、配置だけでなく、それぞれが使える横幅も考える必要がある。

### 2. 固定表示するReserveボタン

画面をスクロールしても同じ位置へ表示する要素には、CSSの`position: fixed`を使用できる。

```css
.floating-reserve {
  position: fixed;
  right: -20px;
  bottom: 24px;
}
```

`right`を少しマイナスにすると、ボタンが画面右端から少しはみ出して見える。ただし、文字まで画面外へ出ないことと、スマホでほかの情報を隠さないことを確認する。

### 3. 写真コラージュを横へ流す方法

複数の写真を横一列に並べ、その列全体を移動させると、コラージュの形を保ったままスライドできる。

```text
写真のまとまりを横に並べる
↓
同じまとまりを続けて用意する
↓
列全体をゆっくり横へ動かす
```

写真とフッターを一体に見せたい場合は、間へ強い白背景や大きな余白を入れず、色、速度、写真の高さをそろえることが大切である。

### 4. 画像ファイル内の余白

HTMLやCSSの余白を小さくしても空白が残る場合は、画像そのものに透明部分や白い余白が含まれている可能性がある。

確認する順番は次のとおり。

1. HTMLの要素構造
2. CSSの`margin`と`padding`
3. 画像ファイル内の透明余白

Reserveページでは、この3つを分けて確認することで、アイキャッチ下の空きすぎた印象を調整した。

### 5. 表とカードの見え方の違い

営業案内は、背景色、角丸、影、広い内側余白が重なると「表」より「カード」に見えやすい。

シンプルな表に見せる場合は、表全体を囲む白や薄緑の背景、大きな角丸、影、広すぎる余白を控えめにする。今回は、背景を付けず、曜日の区切り線と正円の印だけを使う案を採用した。

### 6. CSSの後勝ちと試作の残り方

CSSでは、同じ要素への指定が複数ある場合、条件が同程度なら後ろに書かれた指定が優先される。

```css
.weekly-calendar {
  background: #ffffff;
}

.weekly-calendar {
  background: transparent;
}
```

この場合は、後ろの`transparent`が表示される。試作を繰り返すと古い背景指定が残りやすいため、見た目が意図と違うときは、対象のCSSが複数書かれていないか確認する。

### 7. 比較版と基準版を分ける

前の版を上書きせず別フォルダとして残すと、変更がイメージと違った場合でも、すぐ前の状態へ戻せる。

昨日の最後に採用した基準版は次のとおり。

```text
作業版：uribou-buchi-simple-calendar-v19
配布用：uribou-buchi-complete-final-simple-calendar-20260717.zip
```

外枠を追加した格子型の版は背景があるように見えたため、現在の基準には使用しない。

### 8. GitHubへ手動アップロードするときの注意

GitHub Pagesへ更新内容を送るときは、ZIPファイルそのものではなく、ZIPを展開した中のHTML、CSS、JavaScript、画像などをアップロードする。

変更コメントは、何を更新したか後から分かる短い文章にする。

```text
Update site design and add business calendar
```

アップロード後は公開サイトを確認し、古いCSSが残る場合は`Ctrl + F5`で強制再読み込みする。

### 今日の重要ポイント

- ヘッダーとフッターは役割が異なるため、修正対象を最初に確認する
- 固定ボタンは本文やフッターを隠さない配置にする
- 写真は色、間隔、動きの速度をそろえると一体感が出る
- 空白の原因はCSSだけでなく、画像ファイル内にもある
- 背景、角丸、影が強いと、表がカードのように見える
- CSSの試作を重ねたときは、古い指定と記述順を確認する
- 比較版を残すと、採用しなかった変更から安全に戻せる
- GitHubへはZIPではなく、展開した中身をアップロードする

### 制作時間

- 2026年7月15日時点：43時間以上
- 2026年7月17日分：未集計
- 累計：正確な作業時間を確認後に更新する

### 次回の作業

- GitHub Pagesへ反映された全6ページを確認する
- PCとスマホでフッター、固定Reserveボタン、営業案内を確認する
- 予約・お問い合わせフォームの入力項目と送信方法を確認する
- 画像の`alt`とGoogleマップの`title`を最終確認する
- 今後の修正は`uribou-buchi-simple-calendar-v19`を基準にする
